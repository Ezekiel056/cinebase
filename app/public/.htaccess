RewriteEngine On

# Si le fichier existe, Apache le sert normalement
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon tout passe par index.php
RewriteRule ^ index.php [L,QSA]